# 暗棋象棋 · QQ 小程序部署规划书（web-view 方案）

> 目标：把 `D:\xiangqi` 下这套纯前端 H5 暗棋，塞进 QQ 小程序的 `<web-view>`（网页视图）组件上线。
> 结论：**游戏代码几乎零改动**，主要工作量在「买服务器 + 备案 + 配后台」。

---

## 一、架构（部署后长这样）

```
玩家手机 QQ
   ↓ 打开「暗棋象棋」小程序
QQ 小程序壳（qq-miniprogram/，只有一个 web-view 页面）
   ↓ 加载 https://你的域名/index.html
你的云服务器（公网，必须 HTTPS）
   ├─ Nginx（443 端口，负责加密 + 转发）
   │     ↓ 全部请求转发给本机 8080
   └─ server.js（Node，零依赖）
         ├─ 静态文件：index.html / game.js / styles.css / net.js
         └─ 联机 API：/api/join /api/match /api/poll /api/send …（长轮询中转）
```

要点：网页和联机接口**同一个域名、同源**，web-view 里 `net.js` 用的相对路径 `/api/...` 不用改。

---

## 二、基础设施规划（你还没有，需新购）

### 1. 云服务器（必须在中国大陆，否则无法 ICP 备案）
QQ 小程序要求网页域名**已备案**，而备案的前提是服务器来自支持备案的国内云厂商。推荐：

| 厂商 | 推荐机型 | 参考价（首年） | 备注 |
|---|---|---|---|
| 腾讯云 | 轻量应用服务器 2核2G / 4M带宽 | ¥100–300（大促更低） | 和 QQ/微信生态近，备案系统顺手 |
| 阿里云 | 轻量应用服务器 2核2G | ¥100–300 | 同样支持备案 |

> 暗棋是低并发的小游戏（每局 2 人 + 少量观战），2核2G 足够撑几千人在线。别买贵了。

### 2. 域名
- 在国内注册商（腾讯云/阿里云）买一个，如 `anqi.com` / `xxx.cn`。
- 价：¥50–100/年。
- **必须和服务器同一家或支持备案接入**（新买的统一在腾讯云办最省事）。

### 3. SSL 证书（HTTPS 加密，强制要求）
- 腾讯云/阿里云提供**免费 DV 证书**，或用 Let's Encrypt 免费证。
- 不用花钱。

### 4. 小程序账号
- QQ 小程序后台（q.qq.com / mp.qq.com）注册，做**企业认证**（你正在办石家庄公司，用营业执照认证）。
- 企业认证通常有一次性认证费（参考微信侧约 ¥300，以 QQ 官方为准）。

### 首年成本粗估
域名 ¥100 + 服务器 ¥200 + 认证 ¥300 ≈ **¥600 左右**（不含你的人力）。

---

## 三、ICP 备案流程（最耗时，约 1–3 周，卡住一切）

1. 在腾讯云买好**服务器**和**域名**。
2. 进入腾讯云「备案」系统，选「企业备案」，填营业执照、法人/负责人身份证、手机号、服务器 IP。
3. 按提示做**真实性核验**（手机小程序人脸或邮寄幕布拍照，现在多是人脸）。
4. 腾讯云初审 → 提交管局（省通信管理局）→ 短信核验 → 下发备案号。
5. 拿到备案号后，在网站页脚加上「备案号 XXX」。

> 提示：用**公司营业执照**备案（企业备案），个人备案不允许明显商业经营。和你注册公司计划一致，正合适。

---

## 四、服务器部署步骤（备案通过后做）

### 步骤 1：装环境
服务器装 Node.js（≥18，和你本机 22 一致即可）和 Nginx。

### 步骤 2：传代码
把 `D:\xiangqi` 整个目录传到服务器，例如 `/var/www/xiangqi`。

### 步骤 3：让 server.js 常驻
用 `pm2`（Node 进程管理）或 systemd 把它跑起来，监听 8080：
```bash
cd /var/www/xiangqi
npm i -g pm2        # 或服务器本地装
pm2 start server.js --name anqi
pm2 save
```

### 步骤 4：Nginx 接管 HTTPS + 转发
`/etc/nginx/conf.d/anqi.conf` 示例：
```nginx
server {
  listen 443 ssl;
  server_name 你的域名.com;
  ssl_certificate     /etc/nginx/cert/fullchain.pem;
  ssl_certificate_key /etc/nginx/cert/privkey.pem;

  location / {
    proxy_pass http://127.0.0.1:8080;   # 全部转给 server.js，静态+API 都它管
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
  }
}
```
然后 `nginx -t && systemctl reload nginx`。

> 这样 server.js 一行不用改，它本来就会 serve 静态文件和 API。

---

## 五、QQ 小程序 web-view 配置

1. 开发者工具导入 `D:\xiangqi\qq-miniprogram`（见该目录 README）。
2. 改两处：
   - `project.config.json` 的 `appid` → 真实 AppID。
   - `pages/index/index.js` 的 `url` → `https://你的域名.com/index.html`。
3. **业务域名白名单**（关键）：QQ 小程序后台「开发设置 → 业务域名」添加你的域名，
   下载校验文件放到服务器网站根目录（即 `index.html` 同级）。
4. 手机 QQ 扫码「预览」自测：能开游戏、能联机、能弹幕。
5. 自测 OK → 后台「提交审核」→ 通过后「发布」。

---

## 六、上线前检查清单

- [ ] 服务器在国内、已买、已装 Node + Nginx
- [ ] 域名已买、已完成 ICP 备案、页脚挂备案号
- [ ] SSL 证书已配，浏览器访问域名显示小锁（HTTPS 绿锁）
- [ ] server.js 用 pm2 常驻，重启服务器能自动拉起
- [ ] 直接浏览器打开 `https://域名/index.html` 能正常玩（先脱离小程序测）
- [ ] 联机/观战/弹幕在浏览器里测通
- [ ] 小程序壳 url 已改成真实域名
- [ ] 业务域名白名单已加 + 校验文件已上传
- [ ] 手机 QQ 扫码预览：游戏、联机、弹幕均正常
- [ ] 提交审核并发布

---

## 七、时间线

| 阶段 | 耗时 | 能否并行 |
|---|---|---|
| 买服务器 + 域名 | 1–2 天 | — |
| ICP 备案 | 1–3 周 | 可并行：写小程序壳、本地自测 |
| 部署 server.js + Nginx + HTTPS | 1–2 天 | 备案通过后 |
| 小程序配置 + 自测 + 审核 | 3–7 天 | 备案通过后 |
| **合计到上线** | **约 2–4 周**（卡在备案） | |

---

## 八、附：以后想进微信 / 抖音小程序

同一套 H5 **不用重写**，只要再各建一个小程序壳（同样一个 web-view 页面），
把域名加进对应平台的「业务域名」白名单即可。备案是**按域名**的，一份备案三个平台通用。
这就是 web-view 方案对你"多平台分发"规划最大的价值。

## 九、以后想做"正宗小游戏"（可选，非必须）

若以后要原生 QQ 登录、QQ 好友排行榜（开放数据域）、分享卡片，再考虑把前端
从 DOM 改写成 Canvas 的「QQ 小游戏」原生版。那是另一项大工程，建议先 web-view 跑通变现，再评估。

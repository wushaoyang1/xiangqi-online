# 部署到云（让不在同一 WiFi 的人也能和你对战）

本游戏是**纯前端网页 + 一个零依赖的 Node.js 中转服务器**。只要把这个服务器放到「公网（internet = 互联网）」上，
任何人用浏览器打开你给的地址，就能「随机匹配」或「创建房间」和你对战，**跟在哪、连什么 WiFi 都无关**。

> 暗棋保密架构不变：服务器只是传话筒，完全不懂规则、也不存任何暗棋坐标；对手就算翻代码也看不到你的暗棋。

下面用 **Render**（推荐：免费档、不用绑银行卡、用 GitHub 一键部署）做例子。英文术语后面都跟了中文解释。

> 💡 **如果你已经有阿里云或腾讯云的云服务器（ECS = Elastic Compute Service，弹性计算服务，就是一台 24 小时开机的远程电脑）**，那最省事——
> 既不用注册 GitHub，也不用受 Render 免费档「休眠」的限制，直接把文件传上去跑就行。详见文末「**八、已有阿里云 / 腾讯云服务器（最推荐）**」。
> （GitHub 和云服务器不是一回事：GitHub 只存代码不运行；云服务器才是真正跑 `node server.js` 的地方。）

---

## 一、准备工作（一次性）

### 1. 注册 GitHub 账号
打开 https://github.com 注册一个账号（GitHub = 全球最大的代码托管网站，用来存放你的项目文件）。
记住你的**用户名**。

### 2. 在电脑上安装 Git
打开 https://git-scm.com 下载 Git 并安装（一路点「下一步」即可）。
安装完后，在 `D:\xiangqi` 文件夹里右键 →「在终端中打开」（或 Git Bash Here）。

---

## 二、把代码上传到 GitHub（repository = 代码仓库）

在 `D:\xiangqi` 的终端里，依次输入（每行回车）：

```bash
git init
git add .
git commit -m "first"
```

然后到 GitHub 网页上点 **New repository（新建仓库）**，名字随意（如 `xiangqi-online`），
**不要**勾选「Add a README」，点 Create。

创建好后，GitHub 会显示一串命令。把下面两条里的用户名和仓库名换成你自己的，在终端执行：

```bash
git branch -M main
git remote add origin https://github.com/你的用户名/xiangqi-online.git
git push -u origin main
```

（如果上一步 commit 时报错说没有邮箱，先执行这两行再 commit：
`git config --global user.email "你@邮箱.com"` 和 `git config --global user.name "你的名字"`）

push 成功后再刷新 GitHub 页面，就能看到所有文件了。

---

## 三、在 Render 上一键部署（Web Service = 网页服务）

1. 打开 https://render.com ，点「Sign Up」并用 **GitHub 登录**（这样它能直接读你的仓库）。
2. 登录后点 **New** → **Web Service**。
3. 连接（Connect）你刚建的 `xiangqi-online` 仓库。
4. 按下表填写设置：

   | 项目 | 填什么 | 说明 |
   |------|--------|------|
   | Name | xiangqi-online | 随便起 |
   | Region | **Singapore（新加坡）** | 离国内近、延迟低 |
   | Branch（分支） | main | 刚才 push 的分支 |
   | Instance Type（实例类型） | **Free** | 免费档，不用花钱 |
   | Build Command（构建命令） | 留空 | 本项目无需编译 |
   | Start Command（启动命令） | `node server.js` | 启动服务器的命令 |

   > 端口（PORT）不用管，Render 会自动分配，我们的服务器已经能读取它。

5. 点 **Create Web Service**，等约 1~2 分钟，状态变成 **Live（运行中）**。
6. 页面上方会给出一个地址，形如 `https://xiangqi-online.onrender.com`。
   **这就是你的公网地址，发给任何人都能对战。**

---

## 四、开打

- 把 `https://xiangqi-online.onrender.com` 发给人，对方打开后选「联机对战」即可。
- 点 **🎲 随机匹配**：系统自动给你分配在线对手（像天天象棋）。
- 或点 **＋ 创建房间**：生成一个房间号 + 邀请链接，发给指定好友，他点链接进同一房间。
- 双方都**不需要**连同一个 WiFi。

---

## 五、注意事项

- **免费档会休眠**：Render 免费档在约 15 分钟没人访问后会「睡觉」，第一个人访问时要等约 30~60 秒唤醒，之后就快了。这是正常现象。
- **重启会清空房间**：免费档偶尔重启会清空服务器内存里的房间和匹配队列（重启是平台行为，不影响代码）。遇到「房间没了」重新点一次随机匹配 / 创建房间即可。
- **想永远在线**：把 Instance Type 升级成付费档；或改用 **Fly.io**（需绑银行卡验证，但免费额度也够用，且不会休眠）。

---

## 六、其他可部署平台（备选）

- **Fly.io**：`fly.io`，命令行部署，免费额度大、不休眠，但注册时要绑一张银行卡做验证（不扣费）。
- **Koyeb**：`koyeb.com`，同样支持 GitHub 一键部署，有免费档。
- 部署方式都类似：把仓库连上去，启动命令填 `node server.js` 即可。

---

## 七、本地自测（可选）

不部署也能先在自己电脑试联机：

```bash
cd D:\xiangqi
node server.js
```

浏览器打开 `http://localhost:8080`，再开一个无痕窗口（模拟另一台电脑）打开同一地址，
两边都选「联机对战」→ 同一个房间号，即可对战。
（本地版仅限同一台电脑或两个连同一 WiFi 的设备；要跨 WiFi 必须走上面的云部署。）

---

## 八、已有阿里云 / 腾讯云服务器（最推荐，无需 GitHub）

如果你手里已经有阿里云或腾讯云的 **云服务器 ECS**，那这是**最优解**：
- 不用注册 GitHub、不用绑银行卡；
- 24 小时在线、**不会休眠**；
- 完全自己掌控，延迟也低。

核心区别：GitHub 只是「代码仓库（repository）」用来**存代码**；云服务器才是真正**运行** `node server.js` 的机器，并自带一个**公网 IP（public IP = 公网地址）**，别人用这个 IP 就能连进来。

### 步骤 1：拿到云服务器的「公网 IP」
登录阿里云 / 腾讯云控制台 → 进入「云服务器 ECS / 实例」页面 → 找到你的实例，记下 **公网 IP**（形如 `47.xxx.xxx.xxx` 或 `1xx.xxx.xxx.xxx`）。

### 步骤 2：在云服务器上装 Node.js
- 方式 A（简单）：在云服务器里打开浏览器访问 https://nodejs.org ，下载 **LTS（长期支持版）** 安装包，一路下一步装好。
- 方式 B：用 **宝塔面板（bt.cn）** 一键装（适合不喜欢敲命令的小白），装完在「软件商店」里装 Node.js 即可。
- 装好后，在云服务器终端输入 `node -v`，能看到版本号就成功了。

### 步骤 3：把 `D:\xiangqi` 整个文件夹传上去
- 用 **SFTP 工具**（如 FileZilla，ftp 客户端）连上你的云服务器，把本机 `D:\xiangqi` 文件夹整体上传到一个目录，比如 `/home/xiangqi`。
- 或用宝塔面板的「文件」功能直接上传压缩包再解压。

### 步骤 4：放行端口（最关键，90% 连不上都是这步漏了）
云服务器有**两层防火墙**，两层都要放行 TCP 8080：
1. **安全组（security group = 平台防火墙规则）**：在云厂商控制台 → 实例 → 安全组 → 添加「入站规则（inbound rule）」：协议 TCP、端口 `8080`、授权对象 `0.0.0.0/0`（允许所有人）。
2. **系统防火墙**（如果云服务器装了 Linux 的 firewalld / ufw，或 Windows 防火墙）：同样放行 8080。
   - Linux 示例：`sudo firewall-cmd --add-port=8080/tcp --permanent && sudo firewall-cmd --reload`

### 步骤 5：启动服务器（并保持它一直跑）
在云服务器终端：
```bash
cd /home/xiangqi      # 换成你上传的实际目录
node server.js
```
看到「联机服务器已启动」即可。但这样**关掉终端就会停**。建议用 **pm2** 守护（进程管理器，掉线自动重启）：
```bash
npm install -g pm2     # 安装 pm2（只需一次）
pm2 start server.js    # 启动并守护
pm2 save               # 开机自启
```

### 步骤 6：开打
浏览器打开 `http://你的公网IP:8080`（把 `你的公网IP` 换成步骤 1 记下的地址）。
选「联机对战」→ 随机匹配 / 创建房间，把地址或房间号发给对方即可。**对方无需同 WiFi。**

> 想换端口？启动前设环境变量即可：`PORT=9000 node server.js`，同时把安全组放行改成 9000。

### 和 Render 方案对比
| 对比项 | 阿里云/腾讯云 ECS | Render 免费档 |
|--------|------------------|---------------|
| 是否需要已有账号 | 你已有 ✅ | 需注册 |
| 是否休眠 | 不休眠 ✅ | 约 15 分钟休眠 |
| 稳定性 | 高 ✅ | 免费档偶尔重启 |
| 费用 | 你已付 / 按需 | 免费 |
| 上手难度 | 需传文件+放行端口（略多一步） | GitHub 一键部署（最简单） |

如果你只是想**快速验证产品**，先用 Render 免费档；如果打算**长期运营 / 已经买了云服务器**，直接走本节。
